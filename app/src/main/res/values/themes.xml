<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        The starting window, drawn by the system before the process has a first frame.

        The icon is the adaptive icon's own foreground layer rather than a second copy of
        the mark, so the two cannot drift apart. Its art sits inside 61% of its canvas,
        which is within the 2/3 the splash screen keeps clear, so nothing is clipped.
    -->
    <style name="Theme.Alchemy.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#000000</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Alchemy</item>
    </style>

    <style name="Theme.Alchemy" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
